// Release 0 

// PSUEDOCODE
// Input: array of strings 
// For each string, find the length
// Compare the value of the first string to the others
// If it is larger than all the others, 
// Return that value
// Otherwise, compare the value of the second string to the others
// If it is larger than all the others,
// Return that value
// Continue until 
    // the index of the string is equal to the length of the array
    // the longest string is returned 


var Array = ["long", "longer", "longest"]

for (i = 0; i < Array.length; i++) {
var str = Array[i]
var str2 = Array[i+1]
if str.length < str2.length
var biggest = i + 1
};


